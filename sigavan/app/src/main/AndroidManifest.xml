<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fadergs.edu.br.sigavan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.FullVan">

        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PassageiroActivity" />
        <activity android:name=".MotoristaActivity" />
        <activity android:name=".CadastrarFaculdadeActivity" />
        <activity android:name=".CadastrarPassageiroActivity" />
        <activity android:name=".CompletarCadastroPassageiroActivity"></activity>
    </application>

</manifest>